# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Tool caches
.ruff_cache/
.pytest_cache/
.mypy_cache/

# Bundled studio frontend build (scripts/bundle_studio_assets.sh output;
# wheel-only via pyproject [tool.hatch.build.targets.wheel] artifacts)
src/foundry/studio/_assets/

# Virtual environments
.venv

# Per-project runtime state (eval history, audit) — never committed
projects/*/.foundry/

personal_docs/
# Local secrets (never commit)
.env
.env.*
**/credentials.env
