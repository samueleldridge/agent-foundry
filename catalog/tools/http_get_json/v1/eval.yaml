name: http_get_json_v1_eval
description: Contract check against a test-double http_service connection.
scope: tool
target: catalog/http_get_json@v1
cases:
  - id: get_root
    input:
      path: /
    expected:
      status_code: 200
scorers:
  - kind: user
    name: status_check
threshold: 1.0
deterministic: false
schema_version: 1
